# Shared task-note fixtures

Markdown samples used by:

- Desktop Vitest (`src/lib/taskNotes.test.ts` — inline copies also kept for speed)
- Android JVM tests (`android-tasks/tasknotes`) via system property `tasknotes.fixtures`

Keep in sync with [`docs/task-notes-format.md`](../docs/task-notes-format.md).
