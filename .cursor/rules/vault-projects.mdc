---
description: MarkSpace vault terminology — first-level folders are projects
alwaysApply: true
---

# Vault projects

In MarkSpace, a **project** is a first-level folder directly under the vault root (relative path with no `/`). Nested folders are ordinary folders, not projects.

- Helper: `isVaultProjectFolder(path, isDir)` in `src/lib/vaultApi.ts`.
- Tree UI uses a package icon (`FcPackage`) for projects; nested dirs keep folder icons.
- Language-learning projects with a set **Learning language** show that country's flag icon (SVG; Windows has no flag emoji glyphs) instead of the package icon (tree + chat project picker).
- Diary projects use a planner icon (`FcPlanner`) instead of the package icon (tree + chat project picker).
- Diary projects: context menu **New daily note** (instead of New note) opens or creates today's note at `{project}/{yyyy}/{MM}/{dd.MMM.yyyy}.md` (helpers in `src/lib/diaryNotes.ts`). Toolbar create (+) is disabled when the selected folder is under a diary project. Agent tool: `open_or_create_daily_note` (date `YYYY-MM-DD`); system prompt documents the same layout.
- Prefer the word "project" in product copy and agent language when referring to those top-level folders.

## Project properties

Editable via tree context menu **Project properties…** → dialog.

- Stored in `.markspace/projects/<sha256(path)>.json` (`path`, `about`, `projectType`, `learningLanguage`, `color`).
- **`about`** — UI label **What is this project about**.
- **`projectType`** — `""` (None) | `knowledgeBase` | `languageLearning` | `diary`. Default empty.
- **`learningLanguage`** — ISO 639-1 code (same catalog as native language prefs); only meaningful when `projectType` is `languageLearning` (cleared otherwise). UI label **Learning language**.
- **`color`** — optional Material 500 hex from a fixed palette (`""` = unset). Used for graph note nodes, document tab strips, tree/chat accents. Catalog: `src/lib/projectColors.ts`.
- APIs: `get_project_properties` / `set_project_properties` (Rust `projects.rs`); TS `getProjectProperties` / `setProjectProperties`.
- Remapped on rename/move/delete like favorites.

## Chat project context

Composer toolbar has a **project picker** (left of Ask/Agent). Selection is per chat thread (`projectPath` on the thread file).

When a project is selected, `buildSystemPrompt` includes `Active project:`, optional **Project type** / **Learning language**, and the project **about** description so the agent knows the project context.
